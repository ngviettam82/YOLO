# AutoLabel Specific Requirements
# These are included in the main requirements.txt but listed here for reference

# Core YOLO
ultralytics>=8.0.0              # YOLOv8, YOLOv11 framework

# Image Processing
opencv-python>=4.8.0            # Image reading and drawing
Pillow>=10.0.0                  # Image display in GUI

# GUI
# tkinter is included with Python (no pip install needed)

# Progress and utilities
tqdm>=4.65.0                    # Progress bars
numpy>=1.24.0                   # Array operations

# GPU Support (optional but recommended)
torch>=2.1.0                    # PyTorch (install separately with CUDA)
torchvision>=0.16.0             # Vision utilities

# For GPU acceleration, install PyTorch with CUDA 12.8:
# python -m pip install --force-reinstall torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
